slug: calculator-react
title: Calculator Add (React)
difficulty: easy
language: react
default_runtime_version: "19"
description_md: |
  Implement `Calculator` — two number inputs and a `Result` span showing their sum.
gating_config:
  line_coverage_percent: 80
  checkstyle_max_errors: 0
limits:
  per_test_timeout_seconds: 10
public_tests_meta:
  - name: "sums_inputs"
    description: "Sums inputs"
