slug: sum-typescript
title: Sum Two (TypeScript)
difficulty: easy
language: typescript
default_runtime_version: "5.7"
description_md: |
  Export `sum(a, b)` from `solution.ts` returning the sum of two integers.
gating_config:
  line_coverage_percent: 80
  checkstyle_max_errors: 0
limits:
  per_test_timeout_seconds: 10
  session_duration_minutes: 30
public_tests_meta:
  - name: "adds_small_numbers"
    description: "Adds small numbers"
  - name: "adds_negatives"
    description: "Adds negatives"
