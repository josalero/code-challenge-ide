slug: move-zeroes
title: Move Zeroes
difficulty: easy
language: java
default_runtime_version: "26"
description_md: |
  ## What to do
  Move all `0` to the end while keeping relative order of non-zero elements. Modify `nums` in place; return void.
  
  Read the task and signature carefully. Use the public examples to clarify edge cases before submitting.
  
  ## Constraints
  - Implement only the required API; do not change test files.
  - Hidden tests may include additional edge cases beyond the examples.
  - Use the `Solution` class and static methods unless the starter specifies otherwise.
  
  ## Method to implement
  
  `0`
gating_config:
  line_coverage_percent: 80
  checkstyle_max_errors: 0
limits:
  per_test_timeout_seconds: 10
starter_main_class: com.challenge.Solution
public_tests_meta:
  - name: "verify_behavior_for_move_zeroes"
    description: "Verify behavior for move-zeroes"
