slug: multiply-pipe-angular
title: Multiply Pipe (Angular)
difficulty: easy
language: angular
default_runtime_version: "19"
description_md: |
  Standalone `MultiplyPipe` — `transform(value, factor)` returns numeric product.
gating_config:
  line_coverage_percent: 80
  checkstyle_max_errors: 0
limits:
  per_test_timeout_seconds: 10
public_tests_meta:
  - name: "multiplies"
    description: "Multiplies"
