slug: even-odd-partition
title: Even Odd Partition
difficulty: easy
language: python
default_runtime_version: "3.12"
description_md: |
  Return tuple `(evens, odds)` preserving order within each group.
gating_config:
  line_coverage_percent: 80
limits:
  per_test_timeout_seconds: 10
starter_main_class: solution
public_tests_meta:
  - name: "test_mix"
    description: "Checks that mix"
  - name: "test_empty"
    description: "Checks that empty"
