slug: reverse-pipe-angular
title: Reverse Pipe (Angular)
difficulty: easy
language: angular
default_runtime_version: "19"
description_md: |
  Implement standalone `ReversePipe` — reverses a string (empty string stays empty).
gating_config:
  line_coverage_percent: 80
  checkstyle_max_errors: 0
limits:
  per_test_timeout_seconds: 10
public_tests_meta:
  - name: "reverses_text"
    description: "Reverses text"
  - name: "empty_string"
    description: "Empty string"
