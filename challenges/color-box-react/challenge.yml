slug: color-box-react
title: Color Box (React)
difficulty: easy
language: react
default_runtime_version: "19"
description_md: |
  Implement `ColorBox` — `color` prop sets background on a div with `role="img"` and `aria-label` equal to color.
gating_config:
  line_coverage_percent: 80
  checkstyle_max_errors: 0
limits:
  per_test_timeout_seconds: 10
public_tests_meta:
  - name: "applies_background"
    description: "Applies background"
