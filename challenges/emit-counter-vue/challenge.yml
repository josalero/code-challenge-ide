slug: emit-counter-vue
title: Emit Counter (Vue)
difficulty: easy
language: vue
default_runtime_version: "3.5"
description_md: |
  SFC `CounterPanel` — button click increments count shown in `aria-live` region (emit pattern in same file).
gating_config:
  line_coverage_percent: 80
  checkstyle_max_errors: 0
limits:
  per_test_timeout_seconds: 10
public_tests_meta:
  - name: "increments"
    description: "Increments"
