# Docker / production — Phase 1a contract: health + info only (no prometheus on the wire).
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: never
